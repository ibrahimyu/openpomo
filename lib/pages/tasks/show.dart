import 'package:flutter/material.dart';

class TaskShowPage extends StatelessWidget {
  const TaskShowPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
